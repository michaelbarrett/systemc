//server.cpp
#include "systemc.h"

//server receiver -- receives gaze data
void server :: prc_rx() {
  while(true) {
    
    wait();
  }
}

//server transmitter -- sends image data
void server :: prc_tx() {
  while(true) {
    
    wait();
  }
}
